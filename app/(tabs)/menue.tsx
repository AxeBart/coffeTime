import { TextInput, Button,ScrollView, StatusBar, View } from 'react-native';

import { Style } from '@/constants/Style';

export default function Settings() {
    
    return (
        <>
        <StatusBar barStyle='dark-content' />

        <ScrollView style={[Style.view]}>
           

        </ScrollView>
    </>

    );
}

